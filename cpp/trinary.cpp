#include "trinary.h"

namespace trinary {
    int to_decimal(std::string tri){
        i
        for(char c: tri){
            if((int)c > 2) return 0;

        }
    }
}  // namespace trinary

int main(){
    return 0;
}