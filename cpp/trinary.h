#if !defined(TRINARY_H)
#define TRINARY_H

#include <cmath>
#include <string>

namespace trinary {
    int to_decimal(std::string);
    int to_decimal2(std::string);
}  // namespace trinary

#endif // TRINARY_H