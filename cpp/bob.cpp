#include "bob.h"

namespace bob {
    std::string hey(std::string statement){
        bool all_upper{}, is_quest{false}, valid_ws{true};
        int ws_count{}, lettercount
        if(statement.back() == '?') is_quest = true;
        for(const auto &c: statement){
            if(isalph)
        }
        std::cout << "1. " << all_upper << " 2. " << is_quest << " 3. " << sizeof(char) << std::endl;
        return ""; 
    }
}  // namespace bob
